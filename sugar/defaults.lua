local defaults = {}

defaults.defaultFont = love.graphics.getFont()
defaults.defaultColor = {1, 1, 1, 1}
defaults.defaultBackground = {0, 0, 0, 0}
defaults.defaultBorderColor = {0, 0, 0, 0}
defaults.defaultLineWidth = 1

return defaults