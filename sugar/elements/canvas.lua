-- Creates a new Context, a Context is just a Canvas with paint operations
-- The Node renders like a Box Element with the Context's Canvas acting as a background Texture.
-- It would also have the following properties:
-- - All Box properties
-- - id (string)

local Canvas = {}



return Canvas

